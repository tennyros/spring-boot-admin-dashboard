# App properties
spring.config.activate.on-profile=dev

# MySQL properties
spring.datasource.url=jdbc:mysql://localhost:3306/${DB_MS_SCHEMA_NAME}
spring.datasource.username=${DB_MS_USERNAME}
spring.datasource.password=${DB_MS_PASSWORD}