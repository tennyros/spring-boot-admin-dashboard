# App properties
spring.config.activate.on-profile=test

# MySQL properties
spring.datasource.url=jdbc:mysql://localhost:3306/${DB_MS_SCHEMA_NAME:users_schema}
spring.datasource.username=${DB_MS_USERNAME:root}
spring.datasource.password=${DB_MS_PASSWORD:1234}